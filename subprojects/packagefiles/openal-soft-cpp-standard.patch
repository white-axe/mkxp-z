# Changes the C++ standard for OpenAL Soft from c++17 to gnu++17.
# Otherwise we get compilation errors when building for PowerPC targets.

--- a/meson.build
+++ b/meson.build
@@ -15,7 +15,7 @@ if meson.get_compiler('cpp').has_header('threads.h') and not meson.get_compiler(
   add_project_arguments('-D__STDC_NO_THREADS__=1', language: 'cpp')
 endif
 
-cpp_std = 'cpp_std=c++17'
+cpp_std = 'cpp_std=gnu++17'
 
 cpp = meson.get_compiler('cpp')
 if meson.version().version_compare('>= 0.62')
