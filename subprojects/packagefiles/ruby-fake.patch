# Fixes errors when running the -fake.rb script during the Ruby build process when building Ruby on a Windows computer.

--- a/template/fake.rb.in
+++ b/template/fake.rb.in
@@ -39,9 +39,9 @@ class Object
     <%end%><%=v.inspect%>
 % }
 end
-builddir = File.dirname(File.expand_path(__FILE__))
-srcdir = "<%=arg['srcdir']%>"
-top_srcdir = File.realpath(srcdir, builddir)
+builddir = File.dirname(File.expand_path(__FILE__)); if /mswin|mingw/ =~ RUBY_PLATFORM && builddir.start_with?('/') then builddir = builddir[1].upcase + ':' + builddir[2..] end
+srcdir = "<%=arg['srcdir']%>"; if /mswin|mingw/ =~ RUBY_PLATFORM && srcdir.start_with?('/') then srcdir = srcdir[1].upcase + ':' + srcdir[2..] end
+top_srcdir = File.realpath(srcdir)
 fake = File.join(top_srcdir, "tool/fake.rb")
 eval(File.binread(fake), nil, fake)
 ropt = "-r#{__FILE__}"
